# Types
type User implements Timestamps {
    id: ID!
    name: String!
    email: String!
    password: String!
    createdAt: Date!
    updatedAt: Date!
}

# Inputs
input UserCreateInput {
    name: String!
    email: String!
    password: String!
}

input UserUpdateInput {
    name: String
    email: String
    password: String
}
